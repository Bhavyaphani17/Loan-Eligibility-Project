services:
  - type: web
    name: loan-eligibility
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run updated_interface.py --server.port $PORT --server.address 0.0.0.0
